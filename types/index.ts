export type Geolocation = {
  latitude: number;
  longitude: number;
};

export type Todo = {
  title: string;
  userId: string;
  geolocation: string
  content: string;
  status: string;
  colour:string;
};

